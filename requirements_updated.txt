# Install the standard local app dependencies:
#   pip install -r requirements.txt
#
# Optional AI dependencies live in requirements-ai.txt. They are not required
# for clean local dry-runs because the app has deterministic local insights.

-r requirements.txt
